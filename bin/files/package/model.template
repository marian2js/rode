import { Model } from 'rode/loader';

/**
 * {{ model | capitalize }} Model
 */
export class {{ model | capitalize }} extends Model {

  /**
   * Sample method, converts a string to upper case
   */
  sampleMethod(str) {
    return str.toUpperCase();
  }

}