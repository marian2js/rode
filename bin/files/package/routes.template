import { Router } from 'rode/loader';

var router = new Router();{{ restApi }}
router.base = '/';

/**
 * [GET] /
 * Calls {{ package }}Controller.index
 */
router.add({
  action: 'index'
});

export {router};