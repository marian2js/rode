Example of a simple blog using [rode.js](https://github.com/codexar/rode) and [mongoose](http://mongoosejs.com)
====

Install example dependencies:

    $ npm install
    $ bower install

Run the blog:

    $ node app

Run the tests:

    $ grunt test